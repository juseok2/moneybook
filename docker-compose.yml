services:
  db:
    image: postgres:16
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: moneybook
      POSTGRES_PASSWORD: moneybook
      POSTGRES_DB: moneybook
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
